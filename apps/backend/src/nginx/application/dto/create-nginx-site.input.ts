import type { CreateNginxSiteDto } from '../../domain/types/nginx-site.type';

/** Application input for creating or replacing an Nginx site. */
export type CreateNginxSiteInput = CreateNginxSiteDto;
