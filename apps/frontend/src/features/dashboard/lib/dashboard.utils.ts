// formatBytes / formatUptime live in the shared layer (used by multiple features).
// Re-exported here so existing dashboard imports keep working.
export { formatBytes, formatUptime } from '@/shared/lib';
