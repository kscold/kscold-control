import { LoginForm } from '../features/auth/ui';

/**
 * LoginPage
 * Simple orchestration page for login feature
 *
 * Refactored with FSD architecture
 */
export function LoginPage() {
  return <LoginForm />;
}
